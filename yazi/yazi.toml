# Yazi main config
# https://yazi-rs.github.io/docs/configuration/yazi

[mgr]
ratio = [0, 1, 5]        # 三栏比例：父目录 / 当前 / 预览
sort_by = "natural"
sort_dir_first = true
show_hidden = false
show_symlink = true

[preview]
max_width = 1000
max_height = 1000
